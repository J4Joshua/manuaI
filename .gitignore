# Secrets — never commit
.env
.env.*
!.env.example
# local real-creds file (copied to .env at setup); has Moss key + HF token
/env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Models / large local artifacts (pulled at setup, not committed)
models/
*.gguf
*.safetensors
*.onnx
*.bin

# OS / editor
.DS_Store
.idea/
.vscode/

# Logs / scratch
*.log
.audit/
.claude/scheduled_tasks.lock

# Planning docs — kept local, out of the repo
docs/PRD.md
